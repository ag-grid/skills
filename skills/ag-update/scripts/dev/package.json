{
  "name": "ag-update-scripts",
  "private": true,
  "type": "module",
  "description": "Build and test tooling for the ag-update analysis script. The shipped artifact is the committed analyse-update.js bundle; end users never npm-install this package.",
  "scripts": {
    "build": "node build.mjs",
    "cli": "node --experimental-strip-types src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
