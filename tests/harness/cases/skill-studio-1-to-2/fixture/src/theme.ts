import { studioTheme } from "ag-studio";

// `chartAxisColor` is a Studio v1 chart theme parameter.
export const dashboardTheme = studioTheme.withParams({
  chartAxisColor: "#4A5568",
  chartSeparationLinesColor: "#E2E8F0",
});
